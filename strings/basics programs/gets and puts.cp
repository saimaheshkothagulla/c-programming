#include<stdio.h>
int main()
{
	char string[20];
	printf("enter string..");
	gets(string);//gets is used for reading string..
	printf("string is= ");
	puts(string);//puts is used for printing..
	return 0;
}
